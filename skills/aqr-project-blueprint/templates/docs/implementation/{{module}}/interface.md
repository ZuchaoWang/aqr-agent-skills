# {{Module}} interface

## 1. Files

{{One-line role per file in the module.}}

- `{{filename}}.py` — {{role}}.
- `{{filename}}.py` — {{role}}.

## 2. Public surface

{{For each public function / class / type / endpoint: signature on one line, one-sentence role on the next. Skip internal helpers, trivial accessors, and full type bodies (point at the file instead). Target ~1 page for the whole module.}}

### {{filename}}.py

- `{{function}}({{params}}) -> {{return_type}}`
  {{One-sentence role.}}
- `{{Class}}`
  {{One-sentence role. Fields: see file.}}
